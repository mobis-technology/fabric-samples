let jwt = require('jsonwebtoken');

module.exports = {
    // TODO: Move the secret to environment variables 'process.env...'
    // 'secret': process.env.secret,
    'secret':'iiljgfd3qe!2ze',
    jwt:jwt
};
